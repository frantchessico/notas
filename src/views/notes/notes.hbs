<div class="row justify-content-center">
    {{#each notes}}
    <div class="col-md-3 my-2">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">{{title}} <a href="#"><i class="fas fa-edit"></i></a></h4>
                <p>{{description}}</p>
                <button class="btn btn-danger btn-block btn-sm">Delete</button>
            </div>
        </div>
    </div>

   
     {{/each}}

      {{#else}}
    <div class="card mx-auto">
        <div class="card-body">
         <p>Não existem notas ainda, gravar sua primeira nota.</p>
        </div>
    </div>
    {{/else}}
</div>

    
   